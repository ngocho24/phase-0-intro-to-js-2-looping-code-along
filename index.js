// Code your solutions in this file
// message for each array
const birthDay = ["Johan", "Joy", "Njoki"];

for ( let i = 0; i < birthDay.length; i++){
console.log ("Thank you, " + birthDay[i])
};

// decremental loop using while
function countDown(num) {
    while (num >= 0) {
      console.log(num);
      num--;
    }
  }
  
  countDown(10);
  